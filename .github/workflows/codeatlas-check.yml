# CodeAtlas structural CI gate.
#
# Runs `codeatlas check` on PRs and pushes to main, failing the build on any
# error-severity DMZ violation across all detected projects. On failure,
# uploads the JSON violations report as an artifact and (optionally) posts
# a PR comment summary.
#
# Adapt the trigger branches and Python version to taste. The PR-comment
# step needs `pull-requests: write`; without it, GitHub refuses the API
# call and the step no-ops. Drop the `permissions:` block + that step if
# your org disallows bot comments.

name: codeatlas check

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install CodeAtlas
        run: pip install "codeatlas>=0.6,<1"

      # Cache is an OPTIMISATION — the workflow MUST work without it.
      - name: Cache .codeatlas/
        uses: actions/cache@v4
        with:
          path: .codeatlas
          key: codeatlas-${{ hashFiles('**/Cargo.toml', '**/package.json', '**/pyproject.toml', '**/pubspec.yaml', '**/go.mod') }}-v0.6

      - name: Initialise or refresh CodeAtlas
        run: |
          if [ -f .codeatlas/projects.json ]; then
            codeatlas refresh
          else
            codeatlas init
          fi

      - name: Run codeatlas check (gate)
        run: codeatlas check

      - name: Collect violations JSON
        if: failure()
        run: codeatlas check --format json > violations.json || true

      - name: Upload violations artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: codeatlas-violations
          path: violations.json
          if-no-files-found: ignore

      - name: Comment on PR
        if: github.event_name == 'pull_request' && failure()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let body = '### codeatlas check failed\n\nError-severity DMZ violations detected. See the `codeatlas-violations` artifact for full JSON.';
            try {
              const data = JSON.parse(fs.readFileSync('violations.json', 'utf8'));
              const v = Array.isArray(data) ? data : (data.violations || []);
              if (v.length) body += `\n\n**${v.length} violation(s):**\n` + v.slice(0, 20).map(x => `- \`${x.rule || x.name || 'rule'}\`: ${x.message || x.path || ''}`).join('\n');
            } catch (e) {}
            await github.rest.issues.createComment({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number, body });
